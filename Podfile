# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'push-interactive-app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Ably', ':git => 'https://github.com/ably/ably-cocoa.git', :branch => 'develop'
  # Pods for push-interactive-app

  target 'push-interactive-appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'push-interactive-appUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
